遇到的问题：

要做好错误处理：
    - 如果不满足条件，就要。。。
    - 考虑清楚，continue就会直接跳出这次循环，来到下次循环，如果每次循环都有某些值不存在，那么可能这一页爬不到信息，所以让它=0
    - break会跳出整个循环

爬评论区数据时：
	- 首先找到对应的包：
		- 普通：打开抓包工具，回车当前网页，搜索关键字看包，但是没有对应的页码数据，故这个包不行
		- 特殊：打开抓包工具，直接点击评论区，看到抓包工具出现了变化的包，打开检查，发现有带页码参数的包，且可以搜索到信息
		- 发现包对应的url一致http://ticket.lvmama.com/scenic_front/comment/newPaginationOfComments，只是请求参数不一致

爬取每一个景点的评论时：
	- 有些景点只有一页评论
	- 有些有一百多页
	- 在规定请求参数时出现问题
		- 想到在网页中先匹配出每个景点的末尾页数，把这个值作为变量（要强制转化为int型），再对评论进行分页数据自动爬取

